<Window x:Class="Udp_WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Üzenetküldő (UDP/TCP)" Height="550" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Content="IP cím:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="tbxIP" Text="127.0.0.1" Margin="5"/>

        <Label Grid.Row="1" Content="Port:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="tbxPort" Text="8888" Margin="5"/>

        <Label Grid.Row="2" Content="Üzenet:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="tbxMessage" AcceptsReturn="True" 
                 VerticalScrollBarVisibility="Auto" Height="80" Margin="5" TextWrapping="Wrap"/>

        <Label Grid.Row="3" Content="Mód:" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="3" Grid.Column="1" x:Name="cmbMode" SelectedIndex="0" Margin="5">
            <ComboBoxItem Content="UDP"/>
            <ComboBoxItem Content="TCP"/>
        </ComboBox>

        <Button Grid.Row="4" Grid.Column="0" Content="Küldés" Click="btnSend_Click" 
                Width="100" Margin="5" Padding="5"/>
        <Button Grid.Row="4" Grid.Column="1" x:Name="btnStartListen" Content="Figyelés indítása" 
                Click="btnStartListen_Click" Margin="5" Padding="5"/>

        <ListBox Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" x:Name="lsbGetMessage" 
                 Margin="0,10,0,0" ScrollViewer.VerticalScrollBarVisibility="Auto"/>
    </Grid>
</Window>